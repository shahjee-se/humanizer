interface:
  display_name: "Humanizer"
  short_description: "Repo-specific patterns and workflows for humanizer"
  default_prompt: "Use the humanizer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true